name: lucra
services:
  dashboard:
    build: .
    image: lucra-dashboard:local
    ports:
      - "127.0.0.1:8080:8000"
    env_file:
      - path: .env
        required: false
    restart: unless-stopped
    init: true
